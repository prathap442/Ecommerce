<% if @review.valid? %>
  $('#reviewNotice').html('thank  u for your review');
  $('#reviewList').append('<%= escape_javascript render 'review'%>');
  $('#new_review')[0].reset();
  $('#reviewError').hide();
    setTimeout(function(){
    	$(reviewNotice).slideUp();
    },3000)
   $.growl.notice({ message: "Thnk u for the submission!" })  
<%else%>
   $.growl.error({ message: "There is an error!" });
   $('#reveiwError').html('<%= escape_javascript render 'error'%>');    
<% end%>